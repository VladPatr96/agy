# 🎸 Полная инструкция по вайб-кодингу

> **Вайб-кодинг** (Vibe Coding) — методология разработки, придуманная Андреем Карпатием в 2025 году. Суть: вы описываете **что** нужно сделать на естественном языке, а AI-агент **пишет, тестирует и отлаживает код** за вас. Вы — архитектор и режиссёр, AI — исполнитель.

---

## 📋 Содержание

1. [Философия вайб-кодинга](#1-философия-вайб-кодинга)
2. [Обзор инструментов](#2-обзор-инструментов)
3. [Установка и настройка каждого инструмента](#3-установка-и-настройка)
4. [Файлы контекста — мозг ваших агентов](#4-файлы-контекста)
5. [Единый workflow: от идеи до деплоя](#5-единый-workflow)
6. [Сравнительная таблица инструментов](#6-сравнение-инструментов)
7. [Продвинутые техники](#7-продвинутые-техники)
8. [Чеклист проекта](#8-чеклист-проекта)
9. [12 золотых правил вайб-кодинга](#9-золотые-правила)

---

## 1. Философия вайб-кодинга

```
Традиционная разработка:     Разработчик → пишет код → тестирует → деплоит
Вайб-кодинг:                 Разработчик → описывает цель → AI пишет код → проверяет → итерирует
```

### Ключевые принципы

| Принцип | Описание |
|---------|----------|
| **Намерение вместо синтаксиса** | Вы описываете ЧТО хотите, а не КАК это реализовать |
| **Итеративная петля** | Описал → AI сгенерировал → запустил → увидел результат → уточнил |
| **Человек-в-цикле** | Вы всегда контролируете и ревьюите критичные решения |
| **Декомпозиция** | Сложные задачи разбиваются на мелкие, понятные шаги |
| **Результат > процесс** | Фокус на работающем продукте, не на «красоте» кода |

### Когда вайб-кодинг работает лучше всего

- ✅ Прототипирование и MVP
- ✅ CRUD-приложения, лендинги, дашборды
- ✅ Скрипты автоматизации, ETL-пайплайны
- ✅ Рефакторинг и миграции
- ✅ Написание тестов и документации

### Когда нужен осторожный подход

- ⚠️ Финтех, медицина — критичная бизнес-логика
- ⚠️ Высоконагруженные системы — архитектура требует экспертизы
- ⚠️ Кастомные алгоритмы — AI может галлюцинировать

---

## 2. Обзор инструментов

### 🟦 Google Antigravity (IDE)

- **Тип**: Полноценная IDE (форк VS Code)
- **Модели**: Gemini 3 Pro, Claude Sonnet/Opus, GPT-OSS-120B
- **Сила**: GUI, встроенный браузер, артефакты, визуальная оркестровка
- **Два интерфейса**: Editor View (синхронная работа с 1 агентом) и Manager View (оркестровка N агентов параллельно)
- **Сайт**: [antigravity.google](https://antigravity.google)

---

### 🟩 Gemini CLI (терминал)

- **Тип**: CLI-агент в терминале
- **Модели**: Gemini 2.5 Pro, Gemini 3 Pro/Flash
- **Сила**: Бесплатный tier (60 req/min), 1M контекст, расширения, Google Search grounding
- **GitHub**: [github.com/google-gemini/gemini-cli](https://github.com/google-gemini/gemini-cli)

---

### 🟧 Claude Code (терминал)

- **Тип**: CLI-агент от Anthropic
- **Модели**: Claude Sonnet, Claude Opus
- **Сила**: Глубокое понимание кода, Agent Teams (мульти-агенты), checkpointing, VS Code ext
- **Сайт**: [claude.ai/code](https://claude.ai/code)

---

### 🟪 OpenCode (терминал)

- **Тип**: Open-source CLI-агент
- **Модели**: 75+ провайдеров (OpenAI, Anthropic, Google, Ollama, AWS Bedrock…)
- **Сила**: Полностью open-source, LSP-интеграция, локальные модели, приватность
- **GitHub**: [github.com/opencode-ai/opencode](https://github.com/opencode-ai/opencode)

---

## 3. Установка и настройка

### 🟦 Antigravity

```powershell
# 1. Скачать с antigravity.google
# 2. Установить, пройти setup wizard
# 3. Привязать Google-аккаунт
# 4. Выбрать режим: Agent-assisted (рекомендуется)
# 5. Установить браузерное расширение
```

---

### 🟩 Gemini CLI

```powershell
# Установка через npm
npm install -g @google/gemini-cli

# Или запуск без установки
npx @google/gemini-cli

# Первый запуск — авторизация
gemini
# → выбрать "Login with Google" → следовать инструкциям

# Или через API-ключ
$env:GEMINI_API_KEY="YOUR_API_KEY"
gemini
```

**Ключевые команды:**

| Команда | Описание |
|---------|----------|
| `gemini` | Запуск интерактивной сессии |
| `/init` | Инициализация проекта (создаёт GEMINI.md) |
| `/chat save <tag>` | Сохранить сессию |
| `/chat resume <tag>` | Восстановить сессию |
| `/compress` | Сжать контекст (при большом диалоге) |
| `/tools` | Показать доступные инструменты |
| `/stats` | Статистика использования |
| `/model` | Сменить модель |
| `@path/to/file` | Добавить файл в контекст |
| `!command` | Выполнить shell-команду |

---

### 🟧 Claude Code

```powershell
# Установка
npm install -g @anthropic-ai/claude-code

# Запуск
claude

# Авторизация
# → следовать инструкциям в браузере

# Одноразовый запрос (для скриптов)
claude -p "объясни этот код"

# Пайп из файла
cat app.py | claude -p "найди баги"
```

**Ключевые команды:**

| Команда | Описание |
|---------|----------|
| `claude` | Интерактивная REPL-сессия |
| `claude "запрос"` | Сессия с начальным промптом |
| `claude -p "запрос"` | Одноразовый запрос (SDK-режим) |
| `/init` | Создать CLAUDE.md (контекст проекта) |
| `/help` | Список команд |
| `/rewind` | Откатить к предыдущему чекпоинту |
| `/agents` | Управление агент-командами |
| `@agent-name` | Обращение к конкретному агенту |

---

### 🟪 OpenCode

```powershell
# Установка (Go binary)
# macOS/Linux:
curl -fsSL https://opencode.ai/install | bash

# Windows: скачать бинарник с GitHub Releases
# https://github.com/opencode-ai/opencode/releases

# Запуск
opencode
```

**Пример `opencode.json`:**
```json
{
  "provider": {
    "name": "anthropic",
    "model": "claude-sonnet-4",
    "apiKey": "${ANTHROPIC_API_KEY}"
  },
  "lsp": {
    "enabled": true
  }
}
```

---

## 4. Файлы контекста

> **Файлы контекста — самый мощный инструмент вайб-кодинга.** Они превращают AI из «генералиста» в вашего «коллегу по проекту», который знает стек, конвенции и архитектуру.

### Какой файл для какого инструмента

| Файл | Инструмент | Расположение |
|------|-----------|-------------|
| `GEMINI.md` | Gemini CLI | Корень проекта / `~/.gemini/GEMINI.md` (глобальный) |
| `CLAUDE.md` | Claude Code | Корень проекта / `~/.claude/CLAUDE.md` (глобальный) |
| `AGENTS.md` | Antigravity | Корень проекта |
| `.agent/rules/` | Antigravity | Папка с Markdown-правилами |
| `opencode.json` | OpenCode | Корень проекта |

### Шаблон универсального контекстного файла

```markdown
# Проект: [Название]

## Стек
- Язык: TypeScript / Python / ...
- Фреймворк: React / FastAPI / ...
- БД: PostgreSQL / SQLite / ...
- Пакетный менеджер: npm / pip / ...
- Тесты: Vitest / pytest / ...

## Конвенции
- Стиль кода: [описание]
- Именование файлов: PascalCase для компонентов, camelCase для утилит
- Комментарии: на русском / английском
- Запрещено: использовать `any`, коммитить секреты

## Структура проекта
src/
├── components/   # UI-компоненты
├── services/     # Бизнес-логика
├── utils/        # Утилиты
└── types/        # TypeScript типы

## Важные решения
- [Почему выбрали X вместо Y]
- [Известные ограничения]

## Команды
- `npm start` — запуск dev-сервера
- `npm test` — запуск тестов
- `npm run build` — сборка
```

---

## 5. Единый workflow: от идеи до деплоя

### Фаза 1: Планирование (5 мин)

```markdown
ПРОМПТ для любого инструмента:

"Я хочу создать [описание проекта].
Стек: [технологии].
Разбей задачу на 5-7 шагов, укажи файлы которые нужно создать/изменить.
Начни с первого шага."
```

### Фаза 2: Генерация (основная работа)

**В Antigravity:**
1. Перейти в Manager View
2. Создать задачу → описать цель
3. Включить Planning Mode → ревьюить план
4. Одобрить → агент создаёт код
5. Агент автоматически тестирует в браузере

**В Gemini CLI:**
```
gemini
> Создай REST API на Express + TypeScript для управления задачами (CRUD).
  Используй SQLite через better-sqlite3. Добавь валидацию через zod.
  @package.json
```

**В Claude Code:**
```
claude
> Создай React-компонент TaskBoard с drag-and-drop (react-beautiful-dnd).
  Три колонки: To Do, In Progress, Done. Стилизуй через Tailwind.
  Посмотри существующие компоненты в @src/components/
```

**В OpenCode:**
```
opencode
> Напиши unit-тесты для всех сервисов в src/services/.
  Используй pytest, mock внешние зависимости.
  Покрытие должно быть > 80%.
```

### Фаза 3: Верификация

| Метод | Инструмент |
|-------|-----------|
| Автотесты в браузере | Antigravity (встроенный) |
| CLI-тесты | Gemini CLI (`!npm test`) / Claude Code |
| Ручная проверка | Все |
| Код-ревью AI | Claude Code (`/review`) / Gemini CLI |

### Фаза 4: Итерация

```markdown
"Результат хороший, но:
1. Добавь обработку ошибок в API-эндпоинты
2. Кнопка 'Удалить' должна быть красной
3. Добавь loading-состояния на фронтенде"
```

---

## 6. Сравнение инструментов

| Критерий | Antigravity | Gemini CLI | Claude Code | OpenCode |
|----------|:-----------:|:----------:|:-----------:|:--------:|
| **Тип** | IDE (GUI) | CLI | CLI | CLI |
| **Бесплатный** | ✅ (preview) | ✅ (60 req/min) | ❌ (подписка) | ✅ (свои ключи) |
| **Встроенный браузер** | ✅ | ❌ | ❌ | ❌ |
| **Визуальная оркестровка** | ✅ | ❌ | Частично | ❌ |
| **Мульти-агенты** | ✅ | ❌ | ✅ (Agent Teams) | ✅ (сессии) |
| **Контекстное окно** | ~1M | 1M | ~200K | Зависит от модели |
| **Checkpointing** | ✅ (артефакты) | ✅ (`/chat save`) | ✅ (`/rewind`) | ❌ |
| **Локальные модели** | ❌ | ❌ | ❌ | ✅ (Ollama) |
| **LSP-интеграция** | ✅ (VS Code) | ❌ | ✅ (VS Code ext) | ✅ (нативная) |
| **MCP-серверы** | ✅ | ✅ | ✅ | ✅ |
| **Open Source** | ❌ | ✅ | ❌ | ✅ |
| **Лучше всего для** | Полный цикл | Быстрые задачи | Сложная логика | Приватность |

---

## 7. Продвинутые техники

### 7.1 Параллельная разработка (мульти-инструментная)

**Пример workflow:**
1. **Antigravity** → дизайн и фронтенд (Planning Mode, визуальная верификация)
2. **Claude Code** → бэкенд-логика (глубокое понимание кода, Agent Teams)
3. **Gemini CLI** → тесты и CI/CD (бесплатный, быстрый)
4. **OpenCode** → приватные/на локальной модели задачи

### 7.2 Цепочки промптов (Prompt Chaining)

Результат одного инструмента → вход для другого:

```powershell
# Claude Code генерирует API-спецификацию
claude -p "Создай OpenAPI спецификацию для TODO API" > api-spec.yaml

# Gemini CLI генерирует код по спецификации
gemini
> Сгенерируй Express-сервер по @api-spec.yaml
```

### 7.3 Контекст-файлы как «оркестр»

Создайте единый `AGENTS.md` в проекте, который работает для всех:

```markdown
# AGENTS.md / GEMINI.md / CLAUDE.md

## Проект: TaskMaster Pro
## Важно для всех AI-агентов:
- НЕ менять файлы в src/legacy/ без явного разрешения
- Новые компоненты создавать ТОЛЬКО в src/components/
- Тесты обязательны для каждого нового файла
- Коммиты на русском языке в формате: "feat: описание"
```

### 7.4 Self-Review паттерн

```
Шаг 1 (Agent A): "Напиши функцию сортировки массива объектов по нескольким полям"
Шаг 2 (Agent B): "Проведи код-ревью @src/utils/sort.ts. Найди баги, проблемы производительности"
Шаг 3 (Agent A): "Исправь замечания из ревью: [вставить замечания]"
```

### 7.5 Использование MCP-серверов

Все четыре инструмента поддерживают MCP для подключения к внешним сервисам:

```json
{
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": ["@modelcontextprotocol/server-github"]
    },
    "sqlite": {
      "command": "npx",
      "args": ["@modelcontextprotocol/server-sqlite", "path/to/db.sqlite"]
    }
  }
}
```

---

## 8. Чеклист проекта

```markdown
### Перед началом
- [ ] Решить, какие инструменты использовать для каких задач
- [ ] Установить и авторизоваться в выбранных инструментах
- [ ] Создать проектную папку + `git init`

### Настройка контекста
- [ ] Создать `AGENTS.md` (для Antigravity)
- [ ] Создать `GEMINI.md` (для Gemini CLI)
- [ ] Создать `CLAUDE.md` (для Claude Code)
- [ ] Создать `opencode.json` (для OpenCode)
- [ ] Описать стек, конвенции, структуру, команды

### Разработка
- [ ] Фаза 1: Описать задачу → получить план
- [ ] Ревью плана → уточнения
- [ ] Фаза 2: Генерация кода по шагам
- [ ] Фаза 3: Тесты (авто + ручные)
- [ ] Фаза 4: Итерации по фидбеку

### Финализация
- [ ] Финальный код-ревью (AI + ручной)
- [ ] Тесты проходят? ✅
- [ ] Документация обновлена?
- [ ] Коммит + пуш
```

---

## 9. 12 золотых правил вайб-кодинга

### 1️⃣ Один промпт = одна задача
Не смешивайте «добавь авторизацию и поправь шрифт в футере».

### 2️⃣ Контекст решает всё
Потратьте 10 минут на `AGENTS.md` — сэкономите часы.

### 3️⃣ Декомпозируйте сложное
«Создай Uber» → ❌. «Создай экран регистрации с валидацией полей» → ✅.

### 4️⃣ Всегда используйте Planning Mode для сложных задач
Ревью плана дешевле ревью 50 файлов.

### 5️⃣ Проверяйте результат, а не только код
Запустите, потыкайте, посмотрите в браузере.

### 6️⃣ Итерируйте быстро
Не ждите идеала с первого раза. Опиши → увидь → уточни.

### 7️⃣ Выбирайте правильный инструмент
GUI-задачи → Antigravity. Быстрый скрипт → Gemini CLI. Сложная логика → Claude Code. Приватность → OpenCode.

### 8️⃣ Сохраняйте сессии
`/chat save`, `/rewind`, checkpoints — ваша страховка.

### 9️⃣ Не доверяйте AI слепо
Особенно в: безопасности, финансовой логике, работе с данными пользователей.

### 🔟 Автоматизируйте повторяющееся
Workflows, Skills, Extensions — один раз настроил, всегда работает.

### 1️⃣1️⃣ Используйте мульти-агентов
Параллельная работа 3-4 агентов = x3-4 скорости разработки.

### 1️⃣2️⃣ Документируйте, что сделал AI
Через год вы забудете «вайб» — но `CHANGELOG.md` и комментарии останутся.

---

> **Начните прямо сейчас:** откройте терминал, запустите `gemini` или `claude`, опишите свою ближайшую задачу на естественном языке, и позвольте AI отвести вас к результату. Вайб-кодинг — это навык, который растёт с каждым промптом. 🚀
