# 🏗️ План и схема эффективного использования инструментов разработки

> Комплексный план по выстраиванию workflow разработки с использованием AI-инструментов (Antigravity, Gemini CLI, Claude Code, OpenCode, Codex), MCP-серверов, Skills и Agents.

---

## 📐 Архитектура экосистемы

```
┌─────────────────────────────────────────────────────────────────────┐
│                        🧑 РАЗРАБОТЧИК                               │
│                    (архитектор + оркестратор)                        │
└──────────────────────────────┬──────────────────────────────────────┘
                               │
              ┌────────────────┼────────────────┐
              ▼                ▼                ▼
     ┌────────────┐   ┌──────────────┐   ┌──────────────┐
     │  IDE Layer  │   │  CLI Layer   │   │  CI/CD Layer │
     │ Antigravity │   │ Gemini CLI   │   │ Codex/Claude │
     │             │   │ Claude Code  │   │  (headless)  │
     │             │   │ OpenCode     │   │              │
     │             │   │ Codex CLI    │   │              │
     └──────┬─────┘   └──────┬───────┘   └──────┬───────┘
            │                │                   │
            └────────────────┼───────────────────┘
                             │
              ┌──────────────┼──────────────────┐
              ▼              ▼                  ▼
     ┌─────────────┐  ┌───────────┐   ┌──────────────┐
     │   Skills    │  │    MCP    │   │   Agents     │
     │  Framework  │  │  Servers  │   │  Framework   │
     └──────┬──────┘  └─────┬─────┘   └──────┬───────┘
            │               │                 │
            └───────────────┼─────────────────┘
                            ▼
              ┌──────────────────────────┐
              │   Внешние сервисы        │
              │ GitHub · DB · Cloud ·    │
              │ Search · Файловая система│
              └──────────────────────────┘
```

---

## 1. Инструменты — 6 уровней

### Уровень 1: IDE (визуальная разработка)

| Инструмент | Модель | Установка | Когда использовать |
|-----------|--------|-----------|-------------------|
| **Antigravity** | Gemini 3 Pro, Claude, GPT-OSS | [antigravity.google](https://antigravity.google) | Полный цикл: UI, тесты в браузере, Manager View |

### Уровень 2: CLI-агенты (терминальная разработка)

| Инструмент | Модель | Установка | Когда использовать |
|-----------|--------|-----------|-------------------|
| **Gemini CLI** | Gemini 3 Pro/Flash | `npm i -g @google/gemini-cli` | Быстрые задачи, бесплатный tier, 1M контекст |
| **Claude Code** | Claude Sonnet/Opus | `npm i -g @anthropic-ai/claude-code` | Сложная логика, Agent Teams, глубокий ревью |
| **OpenCode** | 75+ провайдеров + Ollama | `curl -fsSL https://opencode.ai/install \| bash` | Приватность, локальные модели, LSP |
| **Codex CLI** | GPT-5.3-Codex | `npm i -g @openai/codex` | Мультимодальный ввод, Full Auto, Git-интеграция |

### Уровень 3: Skills (специализированные навыки)

```
.agent/skills/my-skill/
├── SKILL.md           ← Инструкции (YAML frontmatter + markdown)
├── scripts/           ← Скрипты (Python, Bash, Node)
├── examples/          ← Примеры
└── resources/         ← Шаблоны, ресурсы
```

### Уровень 4: MCP-серверы (подключение к внешним сервисам)

### Уровень 5: Agents (мульти-агентная оркестровка)

### Уровень 6: CI/CD (headless-агенты в пайплайнах)

---

## 2. MCP-серверы — план подключения

### Что такое MCP

**Model Context Protocol** — открытый стандарт (Anthropic → Agentic AI Foundation), позволяющий AI-агентам подключаться к внешним инструментам и данным через единый интерфейс (JSON-RPC 2.0).

### Топ MCP-серверов для разработки

| MCP-сервер | Назначение | Установка |
|-----------|-----------|-----------|
| **GitHub** | Issues, PR, коммиты | `npx @modelcontextprotocol/server-github` |
| **SQLite** | Локальная БД | `npx @modelcontextprotocol/server-sqlite path/to.db` |
| **PostgreSQL** | Рабочая БД | `npx @modelcontextprotocol/server-postgres` |
| **Filesystem** | Файловая система | `npx @modelcontextprotocol/server-filesystem /path` |
| **Brave Search** | Веб-поиск | `npx @modelcontextprotocol/server-brave-search` |
| **Puppeteer** | Браузерная автоматизация | `npx @modelcontextprotocol/server-puppeteer` |
| **Docker** | Контейнеры | `npx @modelcontextprotocol/server-docker` |
| **MongoDB** | NoSQL-база | `npx @modelcontextprotocol/server-mongodb` |
| **Cloudflare** | CDN / DNS / Workers | `npx @cloudflare/mcp-server` |
| **Terraform** | Infrastructure as Code | `npx @terraform/mcp-server` |

### Конфигурация MCP (универсальная)

```json
{
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": ["@modelcontextprotocol/server-github"],
      "env": { "GITHUB_TOKEN": "${GITHUB_TOKEN}" }
    },
    "sqlite": {
      "command": "npx",
      "args": ["@modelcontextprotocol/server-sqlite", "./data/app.db"]
    },
    "filesystem": {
      "command": "npx",
      "args": ["@modelcontextprotocol/server-filesystem", "./src"]
    }
  }
}
```

### Где размещать конфигурацию

| Инструмент | Файл конфигурации |
|-----------|-------------------|
| Antigravity | Settings → MCP Servers |
| Gemini CLI | `~/.gemini/settings.json` или `/mcp` команда |
| Claude Code | `~/.claude/settings.json` |
| OpenCode | `opencode.json` в проекте |
| Codex CLI | `~/.codex/config.json` или аргументы CLI |

---

## 3. Skills — план создания

### Стандарт Agent Skills

Open-standard (Anthropic + OpenAI + Microsoft + GitHub + Cursor):
- Папка с `SKILL.md` + скрипты + ресурсы
- Портируемость между инструментами
- Загружается **по требованию** (экономит контекст)

### Рекомендуемые Skills для проекта

```
.agent/skills/
├── code-review/           # AI код-ревью перед коммитом
│   └── SKILL.md
├── test-generator/        # Генерация тестов для нового кода
│   └── SKILL.md
├── deploy/                # Деплой на сервер
│   ├── SKILL.md
│   └── scripts/deploy.sh
├── db-migration/          # Создание и применение миграций БД
│   └── SKILL.md
├── api-docs/              # Генерация OpenAPI документации
│   └── SKILL.md
├── i18n/                  # Добавление переводов
│   └── SKILL.md
└── security-audit/        # Аудит безопасности кода
    └── SKILL.md
```

### Шаблон SKILL.md

```markdown
---
name: code-review
description: Проведение код-ревью с фокусом на баги, безопасность и стиль
---

# Code Review

## Когда использовать
Перед каждым коммитом или по запросу `/review`

## Шаги
1. Получить diff текущих изменений: `git diff --cached`
2. Проверить каждый файл на:
   - Потенциальные баги и edge cases
   - Проблемы безопасности (SQL injection, XSS, секреты)
   - Соответствие конвенциям проекта
   - Недостающие тесты
3. Оценить: P0 (блокер) / P1 (важно) / P2 (замечание)
4. Сформировать отчёт в формате:
   ```
   ## Код-ревью
   - [P0] файл:строка — описание
   - [P1] файл:строка — описание
   ```
```

---

## 4. Agents — схема оркестровки

### Паттерны мульти-агентной работы

```
Паттерн 1: ПАРАЛЛЕЛЬНЫЙ (скорость)
┌─────────────┐  ┌─────────────┐  ┌─────────────┐
│  Agent A     │  │  Agent B     │  │  Agent C     │
│  Frontend    │  │  Backend     │  │  Тесты       │
│  (Antigravity│  │  (Claude     │  │  (Gemini     │
│   or Codex)  │  │   Code)      │  │   CLI)       │
└──────┬───────┘  └──────┬───────┘  └──────┬───────┘
       └─────────────────┼─────────────────┘
                         ▼
               ┌──────────────────┐
               │  Human Review    │
               │  + Интеграция    │
               └──────────────────┘

Паттерн 2: КОНВЕЙЕР (качество)
┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐
│ Planner  │→ │ Coder    │→ │ Reviewer │→ │ Tester   │
│ (Claude  │  │ (Codex/  │  │ (Claude  │  │ (Gemini  │
│  Opus)   │  │  Gemini) │  │  Code)   │  │  CLI)    │
└──────────┘  └──────────┘  └──────────┘  └──────────┘

Паттерн 3: СПЕЦИАЛИСТЫ (сложность)
              ┌──────────────┐
              │  Orchestrator │  ← Вы или Lead Agent
              └──────┬───────┘
         ┌───────────┼───────────┐
         ▼           ▼           ▼
    ┌─────────┐ ┌─────────┐ ┌─────────┐
    │ DB Expert│ │ UI Expert│ │Security │
    │ (Claude) │ │(Antigrav)│ │ Expert  │
    └─────────┘ └─────────┘ └─────────┘
```

### Как реализовать мульти-агентов в каждом инструменте

| Инструмент | Механизм | Пример |
|-----------|---------|--------|
| **Antigravity** | Manager View → несколько задач | Каждая задача = отдельный агент |
| **Claude Code** | Agent Teams (`/agents`) | `@backend-agent "создай API"` |
| **Codex CLI** | Multi-agent workflows + Agents SDK | Параллельные worktrees |
| **Gemini CLI** | Несколько терминалов + `/chat save` | Разные сессии для разных модулей |
| **OpenCode** | Multi-session | Несколько экземпляров параллельно |

---

## 5. Codex — детальный план использования

### Установка

```powershell
npm install -g @openai/codex

# Запуск
codex

# Авторизация — ChatGPT аккаунт или API ключ
```

### Три режима работы

| Режим | Описание | Команды агента |
|-------|---------|----------------|
| **Suggest** | Предлагает изменения, ничего не трогает | Только чтение |
| **Auto Edit** | Правит файлы, спрашивает перед shell | Файлы ✅, Shell → подтверждение |
| **Full Auto** | Полная автономия в песочнице (без сети) | Всё ✅ (sandbox) |

### Ключевые фичи Codex

- **Мультимодальный ввод** — скриншоты, диаграммы, текст
- **Git-интеграция** — изолированные worktrees, checkpoints
- **Skills Catalog** — аналог Agent Skills
- **Code Review** — автоматический ревью перед коммитом
- **Web Search** — поиск актуальных решений

### Команды Codex CLI

| Команда | Описание |
|---------|----------|
| `codex` | Интерактивная сессия |
| `codex "задача"` | Сессия с начальным промптом |
| `codex --model gpt-5.3-codex` | Выбор модели |
| `codex --mode full-auto` | Переключение в Full Auto |
| `codex --approval-mode auto-edit` | Автоправки с подтверждением shell |

---

## 6. Единая схема workflow проекта

```
  ФАЗА 0: ИНИЦИАЛИЗАЦИЯ                        ФАЗА 1: ПЛАНИРОВАНИЕ
  ─────────────────────                         ────────────────────
  ┌──────────────────┐                          ┌──────────────────┐
  │ 1. git init      │                          │ 1. Описать задачу│
  │ 2. AGENTS.md     │  ─────────────────────►  │ 2. Получить план │
  │ 3. GEMINI.md     │                          │ 3. Ревью плана   │
  │ 4. CLAUDE.md     │                          │ 4. Уточнения     │
  │ 5. MCP setup     │                          └────────┬─────────┘
  │ 6. Skills setup  │                                   │
  └──────────────────┘                                   ▼

  ФАЗА 2: РАЗРАБОТКА (параллельно)              ФАЗА 3: ВЕРИФИКАЦИЯ
  ─────────────────────────────────              ──────────────────────
  ┌───────────┐ ┌───────────┐                   ┌──────────────────────┐
  │ Antigrav. │ │ Claude    │                   │ 1. Unit-тесты        │
  │ Frontend  │ │ Backend   │  ──────────────►  │ 2. E2E (браузер)     │
  └───────────┘ └───────────┘                   │ 3. Code Review (AI)  │
  ┌───────────┐ ┌───────────┐                   │ 4. Security Audit    │
  │ Codex     │ │ Gemini    │                   └────────┬─────────────┘
  │ Infra     │ │ Docs/Tests│                            │
  └───────────┘ └───────────┘                            ▼

  ФАЗА 4: ИТЕРАЦИЯ                              ФАЗА 5: ДЕПЛОЙ
  ─────────────────                              ──────────────────
  ┌──────────────────┐                           ┌──────────────────┐
  │ Фидбек → уточне- │                          │ 1. Build          │
  │ ние → регенерация│  ─────── (повтор) ──────► │ 2. Deploy (Skill) │
  │ → ревью → тесты  │                          │ 3. Healthcheck    │
  └──────────────────┘                           │ 4. Monitor        │
                                                 └──────────────────┘
```

---

## 7. Матрица: какой инструмент для какой задачи

| Задача | Лучший инструмент | Альтернатива | Почему |
|--------|-------------------|-------------|--------|
| **UI/Frontend** | Antigravity | Codex | Встроенный браузер, визуальное ревью |
| **Backend API** | Claude Code | Codex | Глубокое понимание кода, Agent Teams |
| **Тесты** | Gemini CLI | Claude Code | Бесплатный, быстрый, 1M контекст |
| **DevOps/Infra** | Codex | Gemini CLI | Full Auto + sandbox, Git worktrees |
| **Документация** | Gemini CLI | Claude Code | Быстрый, большой контекст |
| **Рефакторинг** | Claude Code | Antigravity | Глубокий анализ зависимостей |
| **Безопасность** | Claude Code | Codex | Аудит, Code Review |
| **Прототип/MVP** | Antigravity | Codex | Planning Mode + браузер + артефакты |
| **Приватный код** | OpenCode | — | Локальные модели, никуда не уходит |
| **CI/CD пайплайн** | Codex (headless) | Claude Code `-p` | Неинтерактивный режим |
| **Данные/Analytics** | Codex | Gemini CLI | Code Execution Tool (Python sandbox) |
| **Legacy-код** | Claude Code | Gemini CLI | Лучший анализ больших кодовых баз |

---

## 8. Конфигурационные файлы проекта — чеклист

```
project-root/
├── .git/
├── AGENTS.md               ← Antigravity (стек, конвенции, структура)
├── GEMINI.md               ← Gemini CLI (= AGENTS.md, можно симлинк)
├── CLAUDE.md               ← Claude Code (= AGENTS.md, можно симлинк)
├── opencode.json            ← OpenCode (провайдер, модель, LSP)
├── .codex/
│   └── instructions.md      ← Codex CLI (инструкции проекта)
├── .agent/
│   ├── rules/               ← Правила для Antigravity
│   │   ├── code-style.md
│   │   └── security.md
│   ├── workflows/           ← Workflows (/test, /review, /deploy)
│   │   ├── test.md
│   │   ├── review.md
│   │   └── deploy.md
│   └── skills/              ← Skills (открытый стандарт)
│       ├── code-review/
│       ├── test-generator/
│       └── deploy/
├── mcp-config.json          ← MCP-серверы (GitHub, DB, Search)
└── src/
```

### Трюк: единый контекстный файл

```powershell
# Создать AGENTS.md как основной, остальные — симлинки
# (чтобы не дублировать содержимое)
New-Item -ItemType SymbolicLink -Path GEMINI.md -Target AGENTS.md
New-Item -ItemType SymbolicLink -Path CLAUDE.md -Target AGENTS.md
```

---

## 9. План внедрения (по шагам)

### Неделя 1: Базовая настройка
- [ ] Установить все 5 инструментов
- [ ] Авторизоваться в каждом
- [ ] Создать тестовый проект
- [ ] Написать AGENTS.md + симлинки
- [ ] Попробовать каждый инструмент на простой задаче

### Неделя 2: MCP и Skills
- [ ] Подключить 3 MCP-сервера (GitHub, Filesystem, SQLite)
- [ ] Создать 2 Skills (code-review, test-generator)
- [ ] Создать 2 Workflows (/test, /review)
- [ ] Протестировать на реальном проекте

### Неделя 3: Мульти-агентная работа
- [ ] Попробовать параллельный паттерн (2 агента)
- [ ] Попробовать конвейерный паттерн
- [ ] Определить «свою» комбинацию инструментов
- [ ] Задокументировать что работает лучше всего

### Неделя 4: Оптимизация
- [ ] Доработать AGENTS.md по опыту
- [ ] Создать дополнительные Skills
- [ ] Настроить headless-агентов в CI/CD
- [ ] Создать командный playbook

---

## 10. Золотые правила экосистемы

1. **AGENTS.md — источник правды.** Потратьте 15 минут — сэкономите недели.
2. **MCP > копипаста.** Подключите БД через MCP, а не копируйте данные в промпт.
3. **Skills > повторение.** Если делаете что-то 3+ раз — создайте Skill.
4. **Параллельность > последовательность.** 3 агента одновременно = x3 скорость.
5. **Правильный инструмент > лучший инструмент.** Каждая задача — свой CLI.
6. **Ревью плана > ревью кода.** Planning Mode экономит часы отладки.
7. **Git — ваша страховка.** Частые коммиты + checkpoints + `/rewind`.
8. **Не доверяйте безопасность AI.** Аудит + человеческий ревью для критичного кода.
