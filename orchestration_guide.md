# 🤖 Автономная оркестрация AI-инструментов разработки

> Как заставить Antigravity, Gemini CLI, Claude Code, Codex и OpenCode работать как **сплочённая команда разработчиков**, где один инструмент управляет другими.

---

## Короткий ответ

**Да, это возможно.** Но на февраль 2026 года полностью автономная кросс-инструментная оркестрация реализуется через **комбинацию механизмов**, а не через одну кнопку. Ниже — все работающие подходы.

---

## 1. Карта возможностей: кто умеет оркестрировать

```
                    ┌──────────────────────────────┐
                    │     УРОВНИ ОРКЕСТРАЦИИ        │
                    └──────────────┬───────────────┘
                                   │
          ┌────────────────────────┼────────────────────────┐
          ▼                        ▼                        ▼
  ┌───────────────┐     ┌──────────────────┐     ┌──────────────────┐
  │  ВНУТРЕННЯЯ   │     │  КРОСС-TOOL      │     │  ФРЕЙМВОРК-      │
  │  (внутри 1    │     │  (один CLI       │     │  УРОВЕНЬ          │
  │   инструмента)│     │   вызывает       │     │  (специальный     │
  │               │     │   другой)        │     │   оркестратор)    │
  └───────┬───────┘     └────────┬─────────┘     └────────┬─────────┘
          │                      │                         │
    ✅ Работает            ✅ Работает               ✅ Работает
    из коробки             через shell/MCP           через SDK
```

---

## 2. Уровень 1: Внутренняя оркестрация (из коробки)

Каждый инструмент **уже умеет** делать мульти-агентную работу **внутри себя**.

### Antigravity — Manager View

```
┌─────────────────────────────────────────────┐
│              MANAGER VIEW                   │
│                                             │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐    │
│  │ Agent 1  │ │ Agent 2  │ │ Agent 3  │    │
│  │ Frontend │ │ Backend  │ │ Тесты    │    │
│  │ ██████░░ │ │ ████░░░░ │ │ ░░░░░░░░ │    │
│  │ 75%      │ │ 50%      │ │ Ожидание │    │
│  └──────────┘ └──────────┘ └──────────┘    │
│                                             │
│  📋 Артефакты · 📝 Планы · 📸 Скриншоты   │
└─────────────────────────────────────────────┘
```

- N параллельных агентов, каждый — своя задача
- Визуальный контроль прогресса
- Общий контекст через `AGENTS.md`

### Claude Code — Agent Teams + Subagents

```
┌──────────────────────────────────────┐
│           CLAUDE CODE                │
│                                      │
│  claude> /agents                     │
│                                      │
│  ┌─────────┐    спавнит    ┌────────┐│
│  │ Main    │──────────────▶│Sub-    ││
│  │ Agent   │               │agent A ││
│  │(Opus 4.6│──┐            └────────┘│
│  │)        │  │  спавнит   ┌────────┐│
│  └─────────┘  └───────────▶│Sub-    ││
│       ▲                    │agent B ││
│       │    результаты      └────────┘│
│       └────────────────────────────┘ │
│                                      │
│  .claude/agents/                     │
│  ├── architect.md    # роль + правила│
│  ├── backend-dev.md                  │
│  ├── code-reviewer.md                │
│  └── tester.md                       │
└──────────────────────────────────────┘
```

```markdown
# .claude/agents/architect.md
Ты — Архитектор проекта.
Роль: Декомпозиция задачи, создание плана, распределение по агентам.
Инструменты: чтение файлов, создание планов.
НЕ пиши код напрямую — делегируй @backend-dev и @frontend-dev.
```

### Codex — Codex App (Command Center)

```
┌──────────────────────────────────────┐
│         CODEX APP (веб)              │
│                                      │
│  ┌─────────────────────────────────┐ │
│  │ Agent Pool                      │ │
│  │                                 │ │
│  │  [Agent 1: feature/auth] ██░░   │ │
│  │  [Agent 2: fix/bug-123]  ████   │ │
│  │  [Agent 3: refactor/api] █████  │ │
│  │                                 │ │
│  │  Каждый в своём Git worktree    │ │
│  └─────────────────────────────────┘ │
│                                      │
│  Параллельные задачи · Git worktrees │
│  Skills · Full Auto sandbox          │
└──────────────────────────────────────┘
```

---

## 3. Уровень 2: Кросс-инструментная оркестрация

> **Ключевая идея:** один AI-агент может вызывать другие инструменты через **shell-команды** и **MCP**.

### Вариант A: Оркестратор через Shell

Любой агент может запускать другие CLI-инструменты:

```
┌──────────────────────────────────────────────┐
│          CLAUDE CODE (оркестратор)            │
│                                              │
│  claude> "Реализуй TODO-приложение:          │
│           1. Сгенерируй API через Codex      │
│           2. Сгенерируй фронт через Gemini   │
│           3. Напиши тесты сам                │
│           4. Интегрируй всё"                 │
│                                              │
│  ┌─────────────────────────────────────┐     │
│  │ !codex -p "создай REST API в       │     │
│  │   src/api/ на Express + TypeScript" │     │
│  └─────────────────────────────────────┘     │
│                     ▼                         │
│  ┌─────────────────────────────────────┐     │
│  │ !gemini -p "создай React-компонент │     │
│  │   TaskList по API-спецификации      │     │
│  │   из @src/api/routes.ts"            │     │
│  └─────────────────────────────────────┘     │
│                     ▼                         │
│  ┌─────────────────────────────────────┐     │
│  │ claude сам: "Напишу интеграционные │     │
│  │   тесты для всех эндпоинтов"       │     │
│  └─────────────────────────────────────┘     │
└──────────────────────────────────────────────┘
```

**Как это работает:**
```bash
# Claude Code может вызвать Codex CLI
codex -p "создай Express API для TODO-приложения в src/api/"

# Claude Code может вызвать Gemini CLI
gemini -p "создай React-фронтенд по @src/api/openapi.yaml"

# Claude Code может вызвать OpenCode
opencode -p "проверь безопасность кода в src/"
```

### Вариант B: Оркестратор через MCP

Codex CLI можно запустить как MCP-сервер — тогда любой инструмент может к нему обращаться:

```json
{
  "mcpServers": {
    "codex": {
      "command": "codex",
      "args": ["--as-mcp-server"]
    },
    "github": {
      "command": "npx",
      "args": ["@modelcontextprotocol/server-github"]
    }
  }
}
```

```
┌──────────────────────────────────┐
│   ANTIGRAVITY (оркестратор)      │
│                                  │
│   MCP: codex   ─── задача ──►  [Codex Agent]
│   MCP: github  ─── PR ──────► [GitHub]
│   MCP: sqlite  ─── query ───► [SQLite]
│                                  │
│   Сам: UI + интеграция           │
└──────────────────────────────────┘
```

### Вариант C: Скрипт-оркестратор (PowerShell/Python)

Для полной автоматизации — скрипт, который управляет всеми инструментами:

```powershell
# orchestrator.ps1 — скрипт-дирижёр

Write-Host "=== Фаза 1: Планирование ==="
claude -p "Прочитай AGENTS.md и создай план для TODO-приложения. Сохрани в plan.md" | Out-File plan_log.txt

Write-Host "=== Фаза 2: Backend (Codex) ==="
codex -p "Реализуй backend API по плану из @plan.md. Фреймворк: Express + TypeScript" | Out-File backend_log.txt

Write-Host "=== Фаза 3: Frontend (Gemini) ==="
gemini -p "Создай React-фронтенд для API из @src/api/routes.ts. Используй Tailwind." | Out-File frontend_log.txt

Write-Host "=== Фаза 4: Тесты (Claude) ==="
claude -p "Напиши unit и integration тесты для всего проекта. Покрытие > 80%." | Out-File tests_log.txt

Write-Host "=== Фаза 5: Ревью (OpenCode) ==="
opencode -p "Проведи код-ревью всех файлов. Найди баги и проблемы безопасности." | Out-File review_log.txt

Write-Host "=== Фаза 6: Исправления (Claude) ==="
claude -p "Прочитай @review_log.txt и исправь все найденные проблемы." | Out-File fixes_log.txt

Write-Host "✅ Готово! Проверь результат."
```

---

## 4. Уровень 3: Фреймворки-оркестраторы

Специализированные фреймворки для построения мульти-агентных систем:

### CrewAI (Python) — «Экипаж» агентов

```python
# crew_dev.py — команда AI-разработчиков
from crewai import Agent, Task, Crew

architect = Agent(
    role="Архитектор",
    goal="Декомпозиция задач и создание плана",
    backstory="Опытный архитектор ПО",
    tools=[shell_tool],  # может вызывать CLI
    llm="claude-opus-4.6"
)

backend_dev = Agent(
    role="Backend-разработчик",
    goal="Реализация API и бизнес-логики",
    tools=[codex_tool, file_tool],
    llm="gpt-5.3-codex"
)

frontend_dev = Agent(
    role="Frontend-разработчик",
    goal="Создание UI-компонентов",
    tools=[gemini_tool, file_tool],
    llm="gemini-3-pro"
)

reviewer = Agent(
    role="Код-ревьюер",
    goal="Найти баги и проблемы",
    tools=[claude_tool],
    llm="claude-sonnet"
)

# Задачи с зависимостями
plan_task = Task(description="Создай план", agent=architect)
backend_task = Task(description="Реализуй API", agent=backend_dev,
                    dependencies=[plan_task])
frontend_task = Task(description="Создай UI", agent=frontend_dev,
                     dependencies=[plan_task])
review_task = Task(description="Проведи ревью", agent=reviewer,
                   dependencies=[backend_task, frontend_task])

crew = Crew(
    agents=[architect, backend_dev, frontend_dev, reviewer],
    tasks=[plan_task, backend_task, frontend_task, review_task],
    process="hierarchical"  # оркестратор управляет
)

result = crew.kickoff()
```

### OpenAI Agents SDK — Handoffs

```python
# agents_orchestrator.py
from openai_agents import Agent, Handoff, Runner

planner = Agent(
    name="Planner",
    instructions="Декомпозируй задачу и делегируй подзадачи.",
    handoffs=[
        Handoff(target="BackendDev", description="Задачи по API"),
        Handoff(target="FrontendDev", description="Задачи по UI"),
    ]
)

backend_dev = Agent(
    name="BackendDev",
    instructions="Реализуй бэкенд через Codex CLI",
    tools=[shell_tool("codex -p")]
)

frontend_dev = Agent(
    name="FrontendDev",
    instructions="Реализуй фронтенд через Gemini CLI",
    tools=[shell_tool("gemini -p")]
)

# Запуск
result = Runner.run(planner, input="Создай TODO-приложение")
```

### Google ADK (Agent Development Kit)

```python
# google_adk_orchestrator.py
from google.adk import Agent, SequentialAgent, ParallelAgent

# Параллельные агенты
parallel_devs = ParallelAgent(
    name="DevTeam",
    sub_agents=[
        Agent(name="BackendAgent", model="gemini-3-pro"),
        Agent(name="FrontendAgent", model="gemini-3-flash"),
    ]
)

# Последовательный пайплайн
pipeline = SequentialAgent(
    name="FullPipeline",
    sub_agents=[
        Agent(name="Planner", model="gemini-3-pro"),
        parallel_devs,
        Agent(name="Reviewer", model="gemini-3-pro"),
    ]
)
```

---

## 5. Полная схема автономной оркестрации

```
┌─────────────────────────────────────────────────────────────────────┐
│                                                                     │
│                    🎯 ЗАДАЧА ОТ РАЗРАБОТЧИКА                        │
│                   "Создай TODO-приложение"                          │
│                                                                     │
└──────────────────────────────┬──────────────────────────────────────┘
                               │
                               ▼
┌──────────────────────────────────────────────────────────────────────┐
│                    🧠 ОРКЕСТРАТОР                                    │
│            (Claude Code / Codex App / CrewAI / AGENTS.md)           │
│                                                                     │
│  1. Читает AGENTS.md (контекст, стек, конвенции)                    │
│  2. Декомпозирует задачу на подзадачи                               │
│  3. Назначает подзадачи специализированным агентам                  │
│  4. Мониторит прогресс                                              │
│  5. Интегрирует результаты                                          │
│  6. Запускает финальную верификацию                                  │
│                                                                     │
└────┬──────────────┬──────────────┬──────────────┬───────────────────┘
     │              │              │              │
     ▼              ▼              ▼              ▼
┌─────────┐  ┌───────────┐  ┌──────────┐  ┌───────────┐
│ Codex   │  │ Gemini    │  │ Claude   │  │ OpenCode  │
│ Backend │  │ Frontend  │  │ Тесты    │  │ Security  │
│         │  │           │  │          │  │ Audit     │
│ shell:  │  │ shell:    │  │ native   │  │ shell:    │
│ codex -p│  │ gemini -p │  │          │  │ opencode  │
└────┬────┘  └─────┬─────┘  └────┬─────┘  └─────┬─────┘
     │             │              │               │
     └─────────────┼──────────────┼───────────────┘
                   │              │
                   ▼              ▼
            ┌────────────┐  ┌──────────┐
            │ Git Merge  │  │ CI/CD    │
            │ + Resolve  │  │ Pipeline │
            └────────────┘  └──────────┘
```

---

## 6. Практический рецепт: «Команда из 4 агентов»

### Шаг 1: Подготовить проект

```powershell
mkdir my-app; cd my-app; git init

# Создать единый контекстный файл
@"
# Проект: my-app (TODO-приложение)
## Стек: React + Express + SQLite + TypeScript
## Конвенции: ESLint, Prettier, комментарии на русском
## Структура: src/api/, src/ui/, src/shared/, tests/
"@ | Out-File AGENTS.md -Encoding utf8

# Симлинки для остальных инструментов
New-Item -ItemType SymbolicLink -Path GEMINI.md -Target AGENTS.md
New-Item -ItemType SymbolicLink -Path CLAUDE.md -Target AGENTS.md
```

### Шаг 2: Запустить оркестратор

```bash
# Claude Code как оркестратор
claude

> Ты — Lead-разработчик. Прочитай AGENTS.md.
  Задача: создать полноценное TODO-приложение.

  Твой план действий:
  1. Создай план разработки и сохрани в plan.md
  2. Делегируй backend Codex: !codex -p "по плану из @plan.md создай API в src/api/"
  3. Делегируй frontend Gemini: !gemini -p "по плану из @plan.md создай UI в src/ui/"
  4. Сам напиши интеграционные тесты
  5. Запусти все тесты: !npm test
  6. Если тесты падают — исправь
  7. Сделай финальный код-ревью

  Работай автономно, отчитывайся после каждого шага.
```

### Шаг 3: Мониторинг (опционально)

```powershell
# Смотреть логи в реальном времени
# Терминал 1: Claude Code (оркестратор) — уже запущен
# Терминал 2: мониторинг файлов
Get-ChildItem -Recurse src/ | Measure-Object -Property Length -Sum
git log --oneline -10
```

---

## 7. Сравнение подходов оркестрации

| Подход | Автономность | Сложность настройки | Лучше для |
|--------|:------------:|:-------------------:|-----------|
| **Antigravity Manager View** | ⭐⭐⭐ | ⭐ (из коробки) | Визуальный контроль, UI-задачи |
| **Claude Code + shell** | ⭐⭐⭐⭐ | ⭐⭐ (промпт) | Быстрая оркестрация |
| **Codex App** | ⭐⭐⭐⭐ | ⭐⭐ (веб-интерфейс) | Множество параллельных задач |
| **PowerShell скрипт** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ (написать скрипт) | Полная автоматизация, CI/CD |
| **CrewAI / Agents SDK** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ (код на Python) | Сложные проекты, продакшн |
| **MCP-мост** | ⭐⭐⭐⭐ | ⭐⭐⭐ (конфигурация) | Интеграция с внешними сервисами |

---

## 8. Ограничения (на февраль 2026)

| Ограничение | Описание | Обходной путь |
|-------------|---------|---------------|
| **Нет единой шины** | Нет стандартного протокола для прямого общения Claude ↔ Codex | Shell-команды, MCP, файлы |
| **Конфликты файлов** | 2 агента могут одновременно изменить один файл | Git worktrees, разделение зон ответственности |
| **Потеря контекста** | При вызове через `-p` теряется история диалога | Передавать `@plan.md` как контекст |
| **Стоимость** | Множество агентов = множество API-запросов | Gemini CLI (бесплатный) для лёгких задач |
| **Координация стилей** | Разные модели пишут в разных стилях | Единый `AGENTS.md` + форматирование после |

---

## 9. Будущее: к чему движемся

```
2025 ──────────── 2026 ──────────── 2027
  │                 │                 │
  │ MCP v1          │ A2A (Agent      │ Полная
  │ Одиночные       │ to Agent)       │ автономная
  │ агенты          │ Кросс-tool      │ оркестрация
  │                 │ оркестрация     │ из коробки
  │                 │ через стандарт  │
  ▼                 ▼                 ▼
```

- **A2A Protocol** (Agent-to-Agent) — Google + партнёры: стандарт для общения агентов напрямую
- **AAIF** (Agentic AI Foundation) — Anthropic + OpenAI + Block: стандартизация MCP
- **Agent Skills Standard** — Anthropic + OpenAI + Microsoft: портируемые навыки
- **VS Code v1.109+** — мульти-агентная платформа, запуск агентов разных провайдеров бок о бок

---

> **Итог:** Автономная оркестрация — это уже реальность. Самый простой путь: **Claude Code как оркестратор** + вызов Codex/Gemini через shell. Самый мощный: **CrewAI / Agents SDK** с программной оркестрацией. К 2027 году это станет стандартной фичей всех инструментов через A2A-протокол. 🚀
